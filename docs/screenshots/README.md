# Screenshots

Drop your own screenshots from the running stand here and they will show up in the
main README (the paths are already referenced):

- `kuma-dashboard.png` — Uptime Kuma with the three monitors green
- `registry-push.png` — a terminal showing `docker push registry.lab.local/...`
- `compose-ps.png` — `docker compose ps` with all services `healthy`
- `docker-version.png` — `docker --version && docker compose version`

Suggested size: 1200–1600 px wide, PNG.
