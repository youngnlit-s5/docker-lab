# Copy to .env and adjust. Consumed by docker compose and the backup container.
RETENTION_DAYS=7
INTERVAL=86400
# Registry basic-auth user (generate the htpasswd file, see README "Setup")
REGISTRY_USER=ilija-s
